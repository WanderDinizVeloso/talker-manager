const isValidEmail = require('./isValidEmail');
const isValidPassword = require('./isValidPassword');
const isValidName = require('./isValidName');
const isValidAge = require('./isValidAge');
const isValidTalk = require('./isValidTalk');
const isValidToken = require('./isValidToken');

module.exports = {
  isValidEmail,
  isValidPassword,
  isValidName,
  isValidAge,
  isValidTalk,
  isValidToken,
};
