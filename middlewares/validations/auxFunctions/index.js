const isLegalAge = require('./isLegalAge');
const isLength = require('./isLength');
const isRequired = require('./isRequired');
const isValid = require('./isValid');

module.exports = {
  isLegalAge,
  isLength,
  isRequired,
  isValid,
};
